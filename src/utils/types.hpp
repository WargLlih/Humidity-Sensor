#pragma once

#include <Arduino.h>

/**
 * @typedef byte
 * @brief An alias for the unsigned 8-bit integer data type (uint8_t).
 *
 * This alias is often used to improve code readability and portability.
 */
typedef uint8_t byte;

/**
 * @typedef string_t
 * @brief An alias for the C-style string (char*) data type.
 *
 * This alias is often used to represent strings in C++ code.
 */
typedef char* string_t;